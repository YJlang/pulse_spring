spring:

  datasource:
    url: jdbc:mysql://localhost:3306/pulse_db?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8&allowPublicKeyRetrieval=true
    username: root
    password: wnsgk677400   # 본인 DB 비밀번호 입력
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
  session:
    jdbc:
      initialize-schema: always

# JWT 설정 (32자 이상 랜덤 문자열)
jwt:
  secret: v3ryS3cr3tK3yF0rPuls3Pr0j3ctM4k3ItL0ng3r!!!
  expiration: 86400000 # 1일

server:
  port: 8080